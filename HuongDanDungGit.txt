pull:                                                                                   push:
git clone <URL>                                                                         git status
                                                                                        // git diff FILE_PATH / git diff
                                                                                        git commit -am ‘YOUR_COMMIT_MESSAGE’
                                                                                        git push -u origin <tên_nhánh>


xem đang ở nhánh nào:                                                                   kiểm tra nhánh:
git branch                                                                              git branch -av
                                                                                        -a (all)
                                                                                        -v (verbose)