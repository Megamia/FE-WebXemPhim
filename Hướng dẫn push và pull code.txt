pull code(lấy code về máy):                                                             push code(đẩy code lên):
git init                                                                                git add . (push toàn bộ code)/git add <tên file> (push 1 phần)
git clone <URL>                                                                         git commit -m "<Thông điệp gửi>"
git pull origin <tên nhánh>                                                             git push origin <tên-nhánh-từ-xa>


thay đổi các thứ trong < > để thực hiện